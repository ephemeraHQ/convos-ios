# Copy this file to .env and fill in your values

# Optional overrides for Local development
# If not set, values will be:
# 1. Auto-detected from your local IP (for Local builds)
# 2. Or use defaults from config.json files
# CONVOS_API_BASE_URL=http://localhost:4000/api/
# XMTP_CUSTOM_HOST=""
